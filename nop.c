#include "monty.h"

/**
 * nop -  do nothing
 * @custom_stack: it does not matter
 * @line_count : for nothing
 *
 * Return: NOTHING
 */
void nop(stack_t **custom_stack, unsigned int line_count)
{
	(void) custom_stack;
	(void) line_count;
}
